#!/usr/bin/env python
from setuptools import setup

setup(
    name="encoder4editing",
    version="0.1",
    description="",
    author="Arne Nix",
    author_email="",
    packages=["encoder4editing"],  # same as name
    install_requires=[],  # external packages as dependencies
)
